import SwiftUI

struct OnboardingView: View {
    var body: some View {
        PlaceholderView(icon: "sparkles", title: "Welcome to Vault", subtitle: "Your premium watch collection manager")
    }
}
