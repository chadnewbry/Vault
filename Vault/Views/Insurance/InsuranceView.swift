import SwiftUI

struct InsuranceView: View {
    var body: some View {
        PlaceholderView(icon: "doc.text.fill", title: "Insurance", subtitle: "Store and manage insurance documents")
            .navigationTitle("Insurance")
    }
}
