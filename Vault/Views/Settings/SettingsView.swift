import SwiftUI

struct SettingsView: View {
    var body: some View {
        PlaceholderView(icon: "gearshape.fill", title: "Settings", subtitle: "Customize your Vault experience")
            .navigationTitle("Settings")
    }
}
